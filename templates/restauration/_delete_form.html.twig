{% extends "base.html.twig" %}
           {% block PageContent %}

               <form method="post" action="{{ path('restauration_delete', {'id': restauration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restauration.id) }}">
    <button class="btn">Delete</button>
</form>

      {% endblock %}
